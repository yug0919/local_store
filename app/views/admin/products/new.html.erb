<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Product</title>
   
</head>
<body>
    <h1 style="text-align: center; color: #2a2a72;">Add New Product</h1>

    <div class="form-container">
        <%= form_with model: @product, url: admin_products_path, local: true do |f| %>
            <p class="notice"><%= notice %></p>       
            <p class="alert"><%= alert %></p>

            <div class="form-group">
                <%= f.label :name %>
                <%= f.text_field :name, class: "input-field" %>
            </div>

            <div class="form-group">
                <%= f.label :description %>
                <%= f.text_area :description, class: "input-field" %>
            </div>

            <div class="form-group">
                <%= f.label :price %>
                <%= f.number_field :price, step: 0.01, class: "input-field" %>
            </div>

            <div class="form-group">
                <%= f.label :stock_quantity %>
                <%= f.number_field :stock_quantity, class: "input-field" %>
            </div>

            <div class="form-group">
                <%= f.label :image_url %>
                <%= f.text_field :image_url, class: "input-field" %>
            </div>

            <div class="form-group">
                <%= f.submit "Create Product", class: "submit-btn" %>
            </div>
        <% end %>
    </div>
</body>
</html>
