<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex justify-center items-center h-screen bg-gray-100">
    <div class="w-full max-w-md bg-white p-8 rounded-2xl shadow-lg text-center">
        <h2 class="text-2xl font-bold text-gray-800 mb-4">Sign Up</h2>
        <p class="text-green-600 font-semibold"><%= notice %></p>
        <p class="text-red-600 font-semibold"><%= alert %></p>
        
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), class: "space-y-4") do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>
          
          <% if resource.is_a?(Customer) %>
          <div class="text-left">
            <%= f.label :name, class: "font-semibold text-gray-700" %>
            <%= f.text_field :name, autofocus: true, autocomplete: "name", class: "w-full p-3 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
          </div>
        
          <div class="text-left">
            <%= f.label :phone, class: "font-semibold text-gray-700" %>
            <%= f.text_field :phone, autocomplete: "tel", class: "w-full p-3 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
          </div>
        
          <div class="text-left">
            <%= f.label :address, class: "font-semibold text-gray-700" %>
            <%= f.text_field :address, autocomplete: "street-address", class: "w-full p-3 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
          </div>
        <% end %>
        
          <div class="text-left">
            <%= f.label :email, class: "font-semibold text-gray-700" %>
            <%= f.email_field :email, autocomplete: "email", class: "w-full p-3 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
          </div>
          <div class="text-left">
            <%= f.label :password, class: "font-semibold text-gray-700" %>
            <% if @minimum_password_length %>
            <em class="text-sm text-gray-500">(<%= @minimum_password_length %> characters minimum)</em>
            <% end %>
            <%= f.password_field :password, autocomplete: "new-password", class: "w-full p-3 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
          </div>
          <div class="text-left">
            <%= f.label :password_confirmation, class: "font-semibold text-gray-700" %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full p-3 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
          </div>
          <div class="mt-4">
            <%= f.submit "Sign up", class: "w-full bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2 rounded-lg transition duration-200" %>
          </div>
        <% end %>
        
        <div class="my-4 text-gray-500">OR</div>
        
        <%= render "devise/shared/links" %>
    </div>
</body>
</html>
