<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex justify-center items-center h-screen bg-gray-100">
    <div class="w-full max-w-md bg-white p-8 rounded-2xl shadow-lg text-center">
     
        <h2 class="text-2xl font-bold text-gray-800 mb-4">
          <%= resource_name == :customer ? "Customer Login" : "Admin Login" %>
        </h2>

        <p class="text-green-600 font-semibold"><%= notice %></p>
        <p class="text-red-600 font-semibold"><%= alert %></p>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name), class: "space-y-4") do |f| %>
          <div class="text-left">
            <%= f.label :email, class: "font-semibold text-gray-700" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full p-3 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
          </div>

          <div class="text-left">
            <%= f.label :password, class: "font-semibold text-gray-700" %>
            <%= f.password_field :password, autocomplete: "current-password", class: "w-full p-3 mt-1 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
          </div>
          
          <% if devise_mapping.rememberable? %>
            <div class="flex items-center space-x-2 mt-2">
              <%= f.check_box :remember_me, class: "w-4 h-4" %>
              <%= f.label :remember_me, class: "text-gray-700" %>
            </div>
          <% end %>
          
          <div class="mt-4">
            <%= f.submit "Log in", class: "w-full bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2 rounded-lg transition duration-200" %>
          </div>
        <% end %>

        <div class="my-4 text-gray-500">OR</div>

        <%= render "devise/shared/links" %>
    </div>
</body>
</html>
